from fen import killer_position, parse_fen
from search import Stupid, search
from game import Game


def main():
    # initial_position = parse_fen(killer_position)
    # bot = Stupid()
    # search(bot, initial_position, print_info=True, depth_limit=5)
    pass


if __name__ == "__main__":
    Game()
